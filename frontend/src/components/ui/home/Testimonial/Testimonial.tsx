import React from "react";

const Testimonial = () => {
   return (
      <section
         id="testimonial"
         className="cs-container"
      >
         Testimonial
      </section>
   );
};

export default Testimonial;
