import React from "react";

const Blogs = () => {
   return (
      <section
         id="blogs"
         className="cs-container"
      >
         Blogs
      </section>
   );
};

export default Blogs;
